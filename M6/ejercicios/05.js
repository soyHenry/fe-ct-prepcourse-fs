// listarPropiedades: Crea una función que reciba un objeto y devuelva un array con todas las propiedades del objeto utilizando Object.keys().

const listarPropiedades = (objeto) => {
  return Object.keys(objeto);
};

module.exports = listarPropiedades;
