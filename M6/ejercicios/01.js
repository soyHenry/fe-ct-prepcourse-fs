// obtenerValorPropiedad: Crea una función que reciba un objeto y el nombre de una propiedad y devuelva su valor utilizando dot notation o bracket notation.

const obtenerValorPropiedad = (objeto, propiedad) => {
  return objeto[propiedad];
};

module.exports = obtenerValorPropiedad;
