/*⚠️ NO MODIFIQUES EL NOMBRE DE LAS DECLARACIONES ⚠️*/
/*3️⃣ EJERCICIO 03 3️⃣*/

function sonIguales(x, y, z, multiplicación) {
   // Retorna true si "x" e "y" son iguales.
   // De lo contrario, retorna false.
   // Tu código:

   y = 7
   b = 31
   j = 62
   c = 7
   multiplicación 

   z = console.log(b === y);
   x = console.log(c === 7);
   return z;
}

function tienenMismaLongitud(str1, str2, fan, fan2, ua, ajúa) {
   // Retorna true si los dos strings tienen la misma longitud.
   // De lo contrario, retorna false.
   // Tu código:
   str1 = 'camión';
   fan = 'sumerced';
    
   fan2 = 'Heredado';
   str1 = 'Junín';

    return (fan == fan2), (str1 == str2)


function menosQueNoventa(num) {
   // Retorna true si el argumento "num" es menor que noventa.
   // De lo contrario, retorna false.
   // Tu código:
   
   num = 75;

   return (90<= num);

}

function mayorQueCincuenta(num)
{
   // Retorna true si el argumento "num" es mayor que cincuenta.
   // De lo contrario, retorna false.
   // Tu código:
num = 104;
return (50>= num);
}

function esPar(num,n,par) {
   // Retorna true si "num" es par.
   // De lo contrario, retorna false.
   // Tu código:

   par = 2*num;
      return par === num;

}



function esImpar(num) 
ffccfrfrf            vvvdv  vvsvwqV{
   // Retorna true si "num" es impar.
   // De lo contrario, retorna false.
   // Tu código:
num = 74;
return 2*num-1 === num;
}


/*⚠️ NO MODIFIQUES NADA DEBAJO DE ESTO ⚠️*/
module.exports = {
   tienenMismaLongitud,
   sonIguales,
   menosQueNoventa,
   mayorQueCincuenta,
   esPar,
   esImpar,
}