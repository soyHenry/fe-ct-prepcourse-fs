function suma(x, y) 
{
   var z = x + y;
   var type = typeof(z);
  
   console.log("Resultado:", type + z); // Muestra el resultado en la consola
  return type + z; // Devuelve el resultado como una cadena
}

module.exports = {
   suma,
};


function suma(x, y)
{
var x = '3'
var y = '8'
var v = x + y 
console.log(v)
return v
}

