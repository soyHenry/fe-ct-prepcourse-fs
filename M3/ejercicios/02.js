//esNumeroEntero: Crea una función que verifique si un número es entero o no y devuelva true si lo es, false si no.

function esNumeroEntero(numero) {
  return Number.isInteger(numero);
}

module.exports = esNumeroEntero;
