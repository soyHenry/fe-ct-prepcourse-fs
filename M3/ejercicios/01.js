//esTipoDato: Crea una función que reciba un argumento y devuelva el tipo de dato (por ejemplo: "string", "number", "boolean", "object", etc.).

function esTipoDato(valor) {
  return typeof valor;
}

module.exports = esTipoDato;
